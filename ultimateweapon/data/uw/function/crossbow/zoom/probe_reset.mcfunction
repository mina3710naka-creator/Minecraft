# ============================================================
#  照準用こうもりを毎ティック視点方向へ瞬時に飛ばし直す（実行者＝こうもり、
#  実行位置＝プレイヤーの視点。まずプレイヤーの現在の視線にtpし直してから
#  最大100ブロックのレイキャストで、その先にいるMOBを探す）
# ============================================================
tp @s ~ ~ ~ ~ ~
scoreboard players set @s uw.sub 100
function uw:crossbow/zoom/probe_step
