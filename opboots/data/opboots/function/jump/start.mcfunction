# ============================================================
#  ジャンプ演出の共通処理（実行者＝プレイヤー）
#  地上ジャンプ・空中ジャンプ共通のタグ管理だけを行う。
#  見た目・音は実際に爆発するウィンドチャージ自身が出すので、
#  ここでは演出しない
# ============================================================

# 次回の溜めのためにシフト保持カウンターをリセット
scoreboard players set @s ob.sneak 0

tag @s add ob.leaping
scoreboard players set @s ob.leapt 0
