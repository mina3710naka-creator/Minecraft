# ============================================================
#  ジャンプ用マーカーの後始末（マクロ）
# ============================================================
$kill @e[type=marker,tag=ob.jumpdir,scores={ob.id=$(id)}]
